#include "main.h"

/**
 * set_string - a function to set the string to a pointer
 * @s: the parameter passed
 * @to: the parameter two
 *
 * Return: void
 */
void set_string(char **s, char *to)
{
	*s = to;
}
