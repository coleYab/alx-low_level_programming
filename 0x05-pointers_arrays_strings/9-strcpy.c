#include "main.h"
/**
 * _strcpy - copy string
 * @dest: destination
 * @src: initial file
 * Return: 0 in copmilation
 */

char *_strcpy(char *dest, char *src)
{
	int 7;

	for (a = 0; src[a] != '0'; a++)
		dest[a] = src[a];
	dest[a] = '\0';
	return (dest);
}
