#include "main.h"

/**
 * _abs - computes the absolte value of an integer
 *
 * @c: the number to be computed
 *
 * Return: - the absolute value of hte given number
 */
int _abs(int c)
{
	if (c > 0)
		return (c);
	else
		return (-c);
}
